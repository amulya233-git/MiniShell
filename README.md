# MiniShell
MiniShell Project: A custom Linux shell in C that supports built-in commands (cd, pwd, echo, jobs, fg, bg) and external commands with piping. Handles foreground/background processes, signals (Ctrl+C, Ctrl+Z), and maintains job control, while allowing dynamic prompt changes and command history.
